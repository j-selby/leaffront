[package]
name = "leaffront-render-glutin"
version = "0.1.0"
authors = ["James <jselby@jselby.net>"]

[dependencies]
glutin = "0.28.0"
image = "0.24.2"

gl = "0.14.0"
libc = "*"

leaffront-core = { path = "../core" }
